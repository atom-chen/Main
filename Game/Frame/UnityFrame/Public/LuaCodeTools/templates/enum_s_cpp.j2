//!!Generate Code,do not edit!!

#pragma once

{% for enum in enums %}
/* {{enum.comment}} */
enum {{enum.name}}
{
    {% for item in enum.cases -%}
    {{enum.name}}_{{ item.name }} = {{ item.value }}, /* {{item.comment}} */
    {% endfor %}
};
{% endfor %}