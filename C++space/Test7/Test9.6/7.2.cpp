#include<iostream>
using namespace std;
/*
void player();
void main()
{
	player();
}
float*  input(int *size);
float avg11(float *array, int size);
void print11(float Avg);

void player()
{
	int *size = new int;
	float *arr = input(size);
	float MyAvg = avg11(arr, *size);
	print11(MyAvg);
	delete size;
}

float*  input(int *size)
{
	*size = 0;
	float *array = new float[10];
	for (int i = 0; i < 10; i++)
	{
		float j = 0;
		cout << "请输入成绩（输入0表示退出）" << endl;
		cin >> j;
		if (j == 0)
		{
			break;
		}
		cout << *size << endl;
		array[i] = j;
		(*size)++;
	}
	return array;
}
float avg11(float *array,int size)
{
	cout << "size=" <<size<< endl;
	float sum = 0;
	for (int i = 0; i < size; i++)
	{
		sum += array[i];
	}
	cout <<"sum="<< sum << endl;
	return sum / size;
}

void print11(const float Avg)
{
	cout <<"平均分="<< Avg << endl;
}
*/