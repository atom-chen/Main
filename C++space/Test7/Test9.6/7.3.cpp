#include<iostream>
using namespace std;


/*
class Box
{
public:
	Box(char* str,float height,float width,float length,float volume)
	{
		cout << "Box()" << endl;
		this->maker = str;
		this->height = height;
		this->width = width;
		this->length = length;
		this->volume = volume;
	}
	char *maker;
	float height;
	float width;
	float length;
	float volume;
protected:
private:
};

void print(Box box)
{
	cout << "名称" << box.maker << "  高度" << box.height << "  宽度" << box.width << "  长度" << box.length << "  体积" << box.volume << endl;
}
void setVo(Box *box)
{
	box->volume = (box->height)*(box->length)*(box->width);
}
void main()
{
	char *a = "Riven";
	Box box(a,10,20,30,40);
	print(box);
	setVo(&box);
	print(box);
}
*/