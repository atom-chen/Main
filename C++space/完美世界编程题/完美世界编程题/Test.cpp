#include<iostream>
#include <string>
using namespace std;

int main()
{
	int size;
	cin >> size;
	if (size < 2 || size>100)
	{
		return 0;
	}
	int *daoda = new int[size];
	int *likai = new int[size];
	//±éÀúÊı×é
	
}