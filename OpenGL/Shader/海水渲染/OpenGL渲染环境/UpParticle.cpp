#include "UpParticle.h"
#include "Time.h"


/*
void UpParticle::SetTexture(GLuint texture)
{
	this->m_Texture = texture;
}

void UpParticle::SetHalfSize(float halfSize)
{
	this->m_HalfSize = halfSize;
}

void UpParticle::Update()
{
	float deltaTime = Time::DeltaTime();
	if (m_LivingTime >= m_LifeTime)
	{
		m_LivingTime = 0;
	}
	m_LivingTime += deltaTime;

	//向上走一段距离
	m_Transform.m_Position.y += m_RotateSpeed*deltaTime;
	m_Transform.m_Position.x += -rand() % 6 + rand() % 6;
}



void UpParticle::Update()
{

}
*/

